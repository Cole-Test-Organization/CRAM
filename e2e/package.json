{
  "name": "cram-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright E2E suite (Phase 4) — drives the real GUI against a live, seeded API on one origin. Orchestrated by dev/scripts/run-e2e-tests.js. See ../TEST-SPEC.md §6–7.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
