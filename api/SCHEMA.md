# Database Schema

> Auto-generated by `npm --prefix api run db:schema` — do **not** edit by hand. Re-run after every migration so this file matches the live database.

- **Database:** `crm`
- **Postgres:** 16.13
- **Generated:** 2026-06-01T15:47:19.272Z
- **Tables:** 23
- **Enums:** 0
- **Views:** 0

---

## Contents

**Tables**

- [`account_contacts`](#account_contacts)
- [`account_details`](#account_details)
- [`account_partners`](#account_partners)
- [`accounts`](#accounts)
- [`agent_sessions`](#agent_sessions)
- [`app_settings`](#app_settings)
- [`contacts`](#contacts)
- [`events`](#events)
- [`meeting_attendees`](#meeting_attendees)
- [`meetings`](#meetings)
- [`notes`](#notes)
- [`opp_products`](#opp_products)
- [`opportunities`](#opportunities)
- [`product_categories`](#product_categories)
- [`products`](#products)
- [`themes`](#themes)
- [`user_agent_settings`](#user_agent_settings)
- [`user_internal_domains`](#user_internal_domains)
- [`user_memories`](#user_memories)
- [`user_theme_settings`](#user_theme_settings)
- [`users`](#users)
- [`vendor_products`](#vendor_products)
- [`vendors`](#vendors)

---

## Tables

### `account_contacts`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `account_id` | `bigint` | NO | — | **PK** |
| `contact_id` | `bigint` | NO | — | **PK** |

**Primary key:** `account_id`, `contact_id`

**Foreign keys:**

- `account_id` → `public.accounts`(`id`) — ON DELETE CASCADE
- `contact_id` → `public.contacts`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_ac_contact` — `CREATE INDEX idx_ac_contact ON public.account_contacts USING btree (contact_id)`

**Row-Level Security:** enabled (forced)

- `account_contacts_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_contacts.account_id)))`
  - WITH CHECK: `((EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_contacts.account_id))) AND (EXISTS ( SELECT 1    FROM contacts c   WHERE (c.id = account_contacts.contact_id))))`

---

### `account_details`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `account_id` | `bigint` | NO | — | **PK** |
| `industry` | `text` | YES | — |  |
| `revenue_usd` | `bigint` | YES | — |  |
| `employee_count` | `integer` | YES | — |  |
| `user_count` | `integer` | YES | — |  |
| `endpoint_count` | `integer` | YES | — |  |
| `server_count` | `integer` | YES | — |  |
| `site_count` | `integer` | YES | — |  |
| `dc_count` | `integer` | YES | — |  |
| `hq_city` | `text` | YES | — |  |
| `hq_state` | `text` | YES | — |  |
| `hq_country` | `text` | YES | — |  |
| `it_team_size` | `integer` | YES | — |  |
| `security_team_size` | `integer` | YES | — |  |
| `soc_model` | `text` | YES | — |  |
| `compliance_frameworks` | `text[]` | NO | `'{}'::text[]` |  |
| `has_ot_environment` | `boolean` | YES | — |  |
| `has_iot_environment` | `boolean` | YES | — |  |
| `firewall_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `edr_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `siem_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `idp_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `mfa_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `pam_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `email_security_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `mdr_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `msp_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `sase_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `sdwan_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `vpn_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `dlp_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `casb_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `vuln_mgmt_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `ticketing_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `productivity_suite_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `cloud_provider_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `technical_notes` | `text` | YES | — |  |
| `last_verified_at` | `timestamp with time zone` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `cspm_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `appsec_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `ndr_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `iot_ot_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |
| `ai_security_ids` | `bigint[]` | NO | `'{}'::bigint[]` |  |

**Primary key:** `account_id`

**Foreign keys:**

- `account_id` → `public.accounts`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_account_details_ai_security_ids` — `CREATE INDEX idx_account_details_ai_security_ids ON public.account_details USING gin (ai_security_ids)`
- `idx_account_details_appsec_ids` — `CREATE INDEX idx_account_details_appsec_ids ON public.account_details USING gin (appsec_ids)`
- `idx_account_details_compliance` — `CREATE INDEX idx_account_details_compliance ON public.account_details USING gin (compliance_frameworks)`
- `idx_account_details_cspm_ids` — `CREATE INDEX idx_account_details_cspm_ids ON public.account_details USING gin (cspm_ids)`
- `idx_account_details_edr_ids` — `CREATE INDEX idx_account_details_edr_ids ON public.account_details USING gin (edr_ids)`
- `idx_account_details_employee_count` — `CREATE INDEX idx_account_details_employee_count ON public.account_details USING btree (employee_count)`
- `idx_account_details_firewall_ids` — `CREATE INDEX idx_account_details_firewall_ids ON public.account_details USING gin (firewall_ids)`
- `idx_account_details_idp_ids` — `CREATE INDEX idx_account_details_idp_ids ON public.account_details USING gin (idp_ids)`
- `idx_account_details_industry` — `CREATE INDEX idx_account_details_industry ON public.account_details USING btree (industry)`
- `idx_account_details_iot_ot_ids` — `CREATE INDEX idx_account_details_iot_ot_ids ON public.account_details USING gin (iot_ot_ids)`
- `idx_account_details_ndr_ids` — `CREATE INDEX idx_account_details_ndr_ids ON public.account_details USING gin (ndr_ids)`
- `idx_account_details_productivity_suite_ids` — `CREATE INDEX idx_account_details_productivity_suite_ids ON public.account_details USING gin (productivity_suite_ids)`
- `idx_account_details_revenue` — `CREATE INDEX idx_account_details_revenue ON public.account_details USING btree (revenue_usd)`
- `idx_account_details_siem_ids` — `CREATE INDEX idx_account_details_siem_ids ON public.account_details USING gin (siem_ids)`
- `idx_account_details_site_count` — `CREATE INDEX idx_account_details_site_count ON public.account_details USING btree (site_count)`

**Row-Level Security:** enabled (forced)

- `account_details_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_details.account_id)))`
  - WITH CHECK: `(EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_details.account_id)))`

---

### `account_partners`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `customer_account_id` | `bigint` | NO | — | **PK** |
| `partner_account_id` | `bigint` | NO | — | **PK** |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `customer_account_id`, `partner_account_id`

**Foreign keys:**

- `customer_account_id` → `public.accounts`(`id`) — ON DELETE CASCADE
- `partner_account_id` → `public.accounts`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `account_partners_check`: `CHECK ((customer_account_id <> partner_account_id))`

**Indexes:**

- `idx_ap_partner` — `CREATE INDEX idx_ap_partner ON public.account_partners USING btree (partner_account_id)`

**Row-Level Security:** enabled (forced)

- `account_partners_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_partners.customer_account_id)))`
  - WITH CHECK: `((EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_partners.customer_account_id))) AND (EXISTS ( SELECT 1    FROM accounts a   WHERE (a.id = account_partners.partner_account_id))))`

---

### `accounts`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('accounts_id_seq'::regclass)` | **PK** |
| `slug` | `text` | NO | — |  |
| `name` | `text` | NO | — |  |
| `status` | `text` | YES | — |  |
| `last_contact` | `date` | YES | — |  |
| `relationship_summary` | `text` | YES | — |  |
| `open_threads` | `jsonb` | YES | — |  |
| `active_deals` | `text` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `user_id` | `bigint` | NO | — |  |
| `domains` | `jsonb` | NO | `'[]'::jsonb` |  |
| `search_vector` | `tsvector` | YES | `to_tsvector('english'::regconfig, ((((((((((COALESCE(slug, ''::text) \|\| ' '::text) \|\| COALESCE(name, ''::text)) \|\| ' '::text) \|\| COALESCE(status, ''::text)) \|\| ' '::text) \|\| COALESCE(relationship_summary, ''::text)) \|\| ' '::text) \|\| COALESCE((open_threads)::text, ''::text)) \|\| ' '::text) \|\| COALESCE((domains)::text, ''::text)))` |  |
| `favorite` | `boolean` | NO | `false` |  |
| `needs_review` | `boolean` | NO | `false` |  |

**Primary key:** `id`

**Unique constraints:**

- `accounts_user_slug_key`: (`user_id`, `slug`)

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `accounts_user_slug_key` *(unique)* — `CREATE UNIQUE INDEX accounts_user_slug_key ON public.accounts USING btree (user_id, slug)`
- `idx_accounts_domains` — `CREATE INDEX idx_accounts_domains ON public.accounts USING gin (domains jsonb_path_ops)`
- `idx_accounts_favorite` — `CREATE INDEX idx_accounts_favorite ON public.accounts USING btree (favorite) WHERE (favorite = true)`
- `idx_accounts_last_contact` — `CREATE INDEX idx_accounts_last_contact ON public.accounts USING btree (last_contact)`
- `idx_accounts_name_trgm` — `CREATE INDEX idx_accounts_name_trgm ON public.accounts USING gin (lower(name) gin_trgm_ops)`
- `idx_accounts_needs_review` — `CREATE INDEX idx_accounts_needs_review ON public.accounts USING btree (needs_review) WHERE (needs_review = true)`
- `idx_accounts_search` — `CREATE INDEX idx_accounts_search ON public.accounts USING gin (search_vector)`
- `idx_accounts_slug` — `CREATE INDEX idx_accounts_slug ON public.accounts USING btree (slug)`
- `idx_accounts_status` — `CREATE INDEX idx_accounts_status ON public.accounts USING btree (status)`
- `idx_accounts_user` — `CREATE INDEX idx_accounts_user ON public.accounts USING btree (user_id)`

**Row-Level Security:** enabled (forced)

- `accounts_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `agent_sessions`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `text` | NO | — | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `title` | `text` | YES | — |  |
| `provider` | `text` | NO | `'local'::text` |  |
| `model` | `text` | NO | `'gemma4:e4b'::text` |  |
| `messages` | `jsonb` | NO | `'[]'::jsonb` |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_agent_sessions_user_updated` — `CREATE INDEX idx_agent_sessions_user_updated ON public.agent_sessions USING btree (user_id, updated_at DESC)`

**Row-Level Security:** enabled (forced)

- `agent_sessions_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `app_settings`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `key` | `text` | NO | — | **PK** |
| `value` | `jsonb` | NO | — |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `key`

---

### `contacts`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('contacts_id_seq'::regclass)` | **PK** |
| `full_name` | `text` | YES | — |  |
| `company` | `text` | YES | — |  |
| `title` | `text` | YES | — |  |
| `email` | `text` | YES | — |  |
| `phone` | `text` | YES | — |  |
| `linkedin` | `text` | YES | — |  |
| `notes` | `text` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `user_id` | `bigint` | NO | — |  |
| `kind` | `text` | NO | `'account'::text` |  |
| `location_raw` | `text` | YES | — |  |
| `city` | `text` | YES | — |  |
| `state` | `text` | YES | — |  |
| `country` | `text` | YES | — |  |
| `search_vector` | `tsvector` | YES | `to_tsvector('english'::regconfig, ((((((((((((((((COALESCE(full_name, ''::text) \|\| ' '::text) \|\| COALESCE(company, ''::text)) \|\| ' '::text) \|\| COALESCE(title, ''::text)) \|\| ' '::text) \|\| COALESCE(email, ''::text)) \|\| ' '::text) \|\| COALESCE(notes, ''::text)) \|\| ' '::text) \|\| COALESCE(location_raw, ''::text)) \|\| ' '::text) \|\| COALESCE(city, ''::text)) \|\| ' '::text) \|\| COALESCE(state, ''::text)) \|\| ' '::text) \|\| COALESCE(country, ''::text)))` |  |

**Primary key:** `id`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `contacts_kind_check`: `CHECK ((kind = ANY (ARRAY['account'::text, 'partner'::text, 'internal'::text])))`

**Indexes:**

- `idx_contacts_city` — `CREATE INDEX idx_contacts_city ON public.contacts USING btree (lower(city))`
- `idx_contacts_country` — `CREATE INDEX idx_contacts_country ON public.contacts USING btree (lower(country))`
- `idx_contacts_email` — `CREATE INDEX idx_contacts_email ON public.contacts USING btree (email)`
- `idx_contacts_full_name_trgm` — `CREATE INDEX idx_contacts_full_name_trgm ON public.contacts USING gin (lower(full_name) gin_trgm_ops)`
- `idx_contacts_kind` — `CREATE INDEX idx_contacts_kind ON public.contacts USING btree (kind)`
- `idx_contacts_name` — `CREATE INDEX idx_contacts_name ON public.contacts USING btree (full_name)`
- `idx_contacts_search` — `CREATE INDEX idx_contacts_search ON public.contacts USING gin (search_vector)`
- `idx_contacts_user` — `CREATE INDEX idx_contacts_user ON public.contacts USING btree (user_id)`

**Row-Level Security:** enabled (forced)

- `contacts_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `events`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('events_id_seq'::regclass)` | **PK** |
| `source` | `text` | NO | — |  |
| `source_id` | `text` | NO | — |  |
| `title` | `text` | NO | — |  |
| `summary` | `text` | YES | — |  |
| `start_date` | `date` | YES | — |  |
| `end_date` | `date` | YES | — |  |
| `mode` | `text` | YES | — |  |
| `location_raw` | `text` | YES | — |  |
| `city` | `text` | YES | — |  |
| `state` | `text` | YES | — |  |
| `country` | `text` | YES | — |  |
| `venue` | `text` | YES | — |  |
| `url` | `text` | YES | — |  |
| `tags` | `jsonb` | NO | `'[]'::jsonb` |  |
| `scraped_at` | `timestamp with time zone` | NO | `now()` |  |
| `first_seen_at` | `timestamp with time zone` | NO | `now()` |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Unique constraints:**

- `events_source_source_id_key`: (`source`, `source_id`)

**Check constraints:**

- `events_mode_check`: `CHECK (((mode IS NULL) OR (mode = ANY (ARRAY['in_person'::text, 'virtual'::text, 'hybrid'::text, 'on_demand'::text]))))`

**Indexes:**

- `events_source_source_id_key` *(unique)* — `CREATE UNIQUE INDEX events_source_source_id_key ON public.events USING btree (source, source_id)`
- `idx_events_city` — `CREATE INDEX idx_events_city ON public.events USING btree (lower(city))`
- `idx_events_country` — `CREATE INDEX idx_events_country ON public.events USING btree (lower(country))`
- `idx_events_mode` — `CREATE INDEX idx_events_mode ON public.events USING btree (mode)`
- `idx_events_start_date` — `CREATE INDEX idx_events_start_date ON public.events USING btree (start_date)`

---

### `meeting_attendees`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `meeting_id` | `bigint` | NO | — |  |
| `contact_id` | `bigint` | YES | — |  |
| `id` | `bigint` | NO | `nextval('meeting_attendees_id_seq'::regclass)` | **PK** |
| `display_name` | `text` | YES | — |  |
| `email` | `text` | YES | — |  |
| `status` | `text` | YES | — |  |

**Primary key:** `id`

**Foreign keys:**

- `contact_id` → `public.contacts`(`id`) — ON DELETE CASCADE
- `meeting_id` → `public.meetings`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `meeting_attendees_identity_chk`: `CHECK (((contact_id IS NOT NULL) OR (display_name IS NOT NULL)))`
- `meeting_attendees_status_check`: `CHECK (((status IS NULL) OR (status = ANY (ARRAY['going'::text, 'declined'::text, 'maybe'::text, 'invited'::text, 'owner'::text]))))`

**Indexes:**

- `idx_ma_contact` — `CREATE INDEX idx_ma_contact ON public.meeting_attendees USING btree (contact_id)`
- `idx_ma_unlinked` — `CREATE INDEX idx_ma_unlinked ON public.meeting_attendees USING btree (meeting_id) WHERE (contact_id IS NULL)`
- `meeting_attendees_linked_uniq` *(unique)* — `CREATE UNIQUE INDEX meeting_attendees_linked_uniq ON public.meeting_attendees USING btree (meeting_id, contact_id) WHERE (contact_id IS NOT NULL)`

**Row-Level Security:** enabled (forced)

- `meeting_attendees_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(EXISTS ( SELECT 1    FROM meetings m   WHERE (m.id = meeting_attendees.meeting_id)))`
  - WITH CHECK: `((EXISTS ( SELECT 1    FROM meetings m   WHERE (m.id = meeting_attendees.meeting_id))) AND ((contact_id IS NULL) OR (EXISTS ( SELECT 1    FROM contacts c   WHERE (c.id = meeting_attendees.contact_id)))))`

---

### `meetings`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('meetings_id_seq'::regclass)` | **PK** |
| `account_id` | `bigint` | YES | — |  |
| `date` | `date` | NO | — |  |
| `title` | `text` | YES | — |  |
| `filename` | `text` | NO | — |  |
| `body` | `text` | NO | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `user_id` | `bigint` | NO | — |  |
| `internal` | `boolean` | NO | `false` |  |
| `needs_review` | `boolean` | NO | `false` |  |
| `search_vector` | `tsvector` | YES | `to_tsvector('english'::regconfig, ((COALESCE(title, ''::text) \|\| ' '::text) \|\| COALESCE(body, ''::text)))` |  |
| `starts_at` | `timestamp with time zone` | YES | — |  |
| `ends_at` | `timestamp with time zone` | YES | — |  |
| `location` | `text` | YES | — |  |

**Primary key:** `id`

**Foreign keys:**

- `account_id` → `public.accounts`(`id`) — ON DELETE CASCADE
- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_meetings_account` — `CREATE INDEX idx_meetings_account ON public.meetings USING btree (account_id)`
- `idx_meetings_date` — `CREATE INDEX idx_meetings_date ON public.meetings USING btree (date)`
- `idx_meetings_internal` — `CREATE INDEX idx_meetings_internal ON public.meetings USING btree (internal) WHERE (internal = true)`
- `idx_meetings_needs_review` — `CREATE INDEX idx_meetings_needs_review ON public.meetings USING btree (needs_review) WHERE (needs_review = true)`
- `idx_meetings_search` — `CREATE INDEX idx_meetings_search ON public.meetings USING gin (search_vector)`
- `idx_meetings_starts_at` — `CREATE INDEX idx_meetings_starts_at ON public.meetings USING btree (starts_at) WHERE (starts_at IS NOT NULL)`
- `idx_meetings_user` — `CREATE INDEX idx_meetings_user ON public.meetings USING btree (user_id)`
- `meetings_account_filename_uniq` *(unique)* — `CREATE UNIQUE INDEX meetings_account_filename_uniq ON public.meetings USING btree (account_id, filename) WHERE (account_id IS NOT NULL)`
- `meetings_internal_filename_uniq` *(unique)* — `CREATE UNIQUE INDEX meetings_internal_filename_uniq ON public.meetings USING btree (user_id, filename) WHERE (account_id IS NULL)`

**Row-Level Security:** enabled (forced)

- `meetings_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `notes`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('notes_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `account_id` | `bigint` | YES | — |  |
| `contact_id` | `bigint` | YES | — |  |
| `opportunity_id` | `bigint` | YES | — |  |
| `body` | `text` | NO | `''::text` |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Foreign keys:**

- `account_id` → `public.accounts`(`id`) — ON DELETE CASCADE
- `contact_id` → `public.contacts`(`id`) — ON DELETE CASCADE
- `opportunity_id` → `public.opportunities`(`id`) — ON DELETE CASCADE
- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `notes_exactly_one_target`: `CHECK ((num_nonnulls(account_id, contact_id, opportunity_id) = 1))`

**Indexes:**

- `idx_notes_account_feed` — `CREATE INDEX idx_notes_account_feed ON public.notes USING btree (account_id, created_at DESC) WHERE (account_id IS NOT NULL)`
- `idx_notes_contact_feed` — `CREATE INDEX idx_notes_contact_feed ON public.notes USING btree (contact_id, created_at DESC) WHERE (contact_id IS NOT NULL)`
- `idx_notes_opp_feed` — `CREATE INDEX idx_notes_opp_feed ON public.notes USING btree (opportunity_id, created_at DESC) WHERE (opportunity_id IS NOT NULL)`
- `idx_notes_user` — `CREATE INDEX idx_notes_user ON public.notes USING btree (user_id)`

**Row-Level Security:** enabled (forced)

- `notes_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `opp_products`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `opportunity_id` | `bigint` | NO | — | **PK** |
| `product_id` | `bigint` | NO | — | **PK** |

**Primary key:** `opportunity_id`, `product_id`

**Foreign keys:**

- `opportunity_id` → `public.opportunities`(`id`) — ON DELETE CASCADE
- `product_id` → `public.products`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_opp_products_product` — `CREATE INDEX idx_opp_products_product ON public.opp_products USING btree (product_id)`

**Row-Level Security:** enabled (forced)

- `opp_products_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(EXISTS ( SELECT 1    FROM opportunities o   WHERE (o.id = opp_products.opportunity_id)))`
  - WITH CHECK: `((EXISTS ( SELECT 1    FROM opportunities o   WHERE (o.id = opp_products.opportunity_id))) AND (EXISTS ( SELECT 1    FROM products p   WHERE (p.id = opp_products.product_id))))`

---

### `opportunities`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('opportunities_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `account_id` | `bigint` | NO | — |  |
| `name` | `text` | NO | — |  |
| `opp_link` | `text` | YES | — |  |
| `trr_link` | `text` | YES | — |  |
| `stage` | `text` | NO | `'opp_identification'::text` |  |
| `notes` | `text` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `search_vector` | `tsvector` | YES | `to_tsvector('english'::regconfig, ((((COALESCE(name, ''::text) \|\| ' '::text) \|\| COALESCE(stage, ''::text)) \|\| ' '::text) \|\| COALESCE(notes, ''::text)))` |  |
| `why_change` | `text[]` | NO | `'{}'::text[]` |  |
| `why_now` | `text[]` | NO | `'{}'::text[]` |  |
| `why_us` | `text[]` | NO | `'{}'::text[]` |  |
| `tech_validation_link` | `text` | YES | — |  |

**Primary key:** `id`

**Foreign keys:**

- `account_id` → `public.accounts`(`id`) — ON DELETE CASCADE
- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `opportunities_stage_check`: `CHECK ((stage = ANY (ARRAY['opp_identification'::text, 'tech_discovery'::text, 'non_pov_tech_validation'::text, 'pov_planning'::text, 'pov_tech_validation'::text, 'tech_decision_pending'::text, 'tech_loss_closed'::text, 'tech_win_closed'::text, 'no_tech_validation_closed'::text])))`

**Indexes:**

- `idx_opportunities_account` — `CREATE INDEX idx_opportunities_account ON public.opportunities USING btree (account_id)`
- `idx_opportunities_search` — `CREATE INDEX idx_opportunities_search ON public.opportunities USING gin (search_vector)`
- `idx_opportunities_stage` — `CREATE INDEX idx_opportunities_stage ON public.opportunities USING btree (stage)`
- `idx_opportunities_user` — `CREATE INDEX idx_opportunities_user ON public.opportunities USING btree (user_id)`

**Row-Level Security:** enabled (forced)

- `opportunities_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `product_categories`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('product_categories_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `name` | `text` | NO | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Unique constraints:**

- `product_categories_user_id_name_key`: (`user_id`, `name`)

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_product_categories_user` — `CREATE INDEX idx_product_categories_user ON public.product_categories USING btree (user_id)`
- `product_categories_user_id_name_key` *(unique)* — `CREATE UNIQUE INDEX product_categories_user_id_name_key ON public.product_categories USING btree (user_id, name)`

**Row-Level Security:** enabled (forced)

- `product_categories_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `products`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('products_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `name` | `text` | NO | — |  |
| `category_id` | `bigint` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Unique constraints:**

- `products_user_id_name_key`: (`user_id`, `name`)

**Foreign keys:**

- `category_id` → `public.product_categories`(`id`) — ON DELETE SET NULL
- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_products_category` — `CREATE INDEX idx_products_category ON public.products USING btree (category_id)`
- `idx_products_user` — `CREATE INDEX idx_products_user ON public.products USING btree (user_id)`
- `products_user_id_name_key` *(unique)* — `CREATE UNIQUE INDEX products_user_id_name_key ON public.products USING btree (user_id, name)`

**Row-Level Security:** enabled (forced)

- `products_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `themes`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('themes_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | YES | — |  |
| `slug` | `text` | NO | — |  |
| `name` | `text` | NO | — |  |
| `description` | `text` | YES | — |  |
| `theme_data` | `jsonb` | NO | — |  |
| `is_builtin` | `boolean` | NO | `false` |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Indexes:**

- `themes_builtin_slug_uniq` *(unique)* — `CREATE UNIQUE INDEX themes_builtin_slug_uniq ON public.themes USING btree (slug) WHERE (user_id IS NULL)`
- `themes_user_id_idx` — `CREATE INDEX themes_user_id_idx ON public.themes USING btree (user_id)`
- `themes_user_slug_uniq` *(unique)* — `CREATE UNIQUE INDEX themes_user_slug_uniq ON public.themes USING btree (user_id, slug) WHERE (user_id IS NOT NULL)`

**Row-Level Security:** enabled (forced)

- `themes_delete` — DELETE, PERMISSIVE, roles: public
  - USING: `((user_id = (current_setting('app.current_user_id'::text, true))::bigint) AND (is_builtin = false))`
- `themes_insert` — INSERT, PERMISSIVE, roles: public
  - WITH CHECK: `((user_id = (current_setting('app.current_user_id'::text, true))::bigint) AND (is_builtin = false))`
- `themes_select` — SELECT, PERMISSIVE, roles: public
  - USING: `((user_id IS NULL) OR (user_id = (current_setting('app.current_user_id'::text, true))::bigint))`
- `themes_update` — UPDATE, PERMISSIVE, roles: public
  - USING: `((user_id = (current_setting('app.current_user_id'::text, true))::bigint) AND (is_builtin = false))`
  - WITH CHECK: `((user_id = (current_setting('app.current_user_id'::text, true))::bigint) AND (is_builtin = false))`

---

### `user_agent_settings`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `user_id` | `bigint` | NO | — | **PK** |
| `provider` | `text` | YES | — |  |
| `model` | `text` | YES | — |  |
| `local_base_url` | `text` | YES | — |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `system_prompt` | `text` | YES | — |  |

**Primary key:** `user_id`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Row-Level Security:** enabled (forced)

- `user_agent_settings_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `user_internal_domains`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `user_id` | `bigint` | NO | — | **PK** |
| `domain` | `text` | NO | — | **PK** |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `user_id`, `domain`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `user_internal_domains_lowercase`: `CHECK ((domain = lower(domain)))`

**Indexes:**

- `idx_user_internal_domains_user` — `CREATE INDEX idx_user_internal_domains_user ON public.user_internal_domains USING btree (user_id)`

**Row-Level Security:** enabled (forced)

- `user_internal_domains_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `user_memories`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('user_memories_id_seq'::regclass)` | **PK** |
| `user_id` | `bigint` | NO | — |  |
| `title` | `text` | YES | — |  |
| `content` | `text` | NO | — |  |
| `enabled` | `boolean` | NO | `true` |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Foreign keys:**

- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Check constraints:**

- `user_memories_content_nonempty`: `CHECK ((length(btrim(content)) > 0))`

**Indexes:**

- `idx_user_memories_user_enabled` — `CREATE INDEX idx_user_memories_user_enabled ON public.user_memories USING btree (user_id, enabled)`

**Row-Level Security:** enabled (forced)

- `user_memories_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `user_theme_settings`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `user_id` | `bigint` | NO | — | **PK** |
| `active_theme_id` | `bigint` | YES | — |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `user_id`

**Foreign keys:**

- `active_theme_id` → `public.themes`(`id`) — ON DELETE SET NULL
- `user_id` → `public.users`(`id`) — ON DELETE CASCADE

**Row-Level Security:** enabled (forced)

- `user_theme_settings_isolation` — ALL, PERMISSIVE, roles: public
  - USING: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`
  - WITH CHECK: `(user_id = (current_setting('app.current_user_id'::text, true))::bigint)`

---

### `users`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('users_id_seq'::regclass)` | **PK** |
| `email` | `text` | NO | — |  |
| `name` | `text` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |
| `disabled_at` | `timestamp with time zone` | YES | — |  |

**Primary key:** `id`

**Unique constraints:**

- `users_email_key`: (`email`)

**Indexes:**

- `users_email_key` *(unique)* — `CREATE UNIQUE INDEX users_email_key ON public.users USING btree (email)`

---

### `vendor_products`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('vendor_products_id_seq'::regclass)` | **PK** |
| `vendor_id` | `bigint` | NO | — |  |
| `name` | `text` | NO | — |  |
| `slug` | `text` | NO | — |  |
| `category` | `text` | NO | — |  |
| `notes` | `text` | YES | — |  |
| `needs_review` | `boolean` | NO | `false` |  |
| `deleted_at` | `timestamp with time zone` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Unique constraints:**

- `vendor_products_vendor_id_slug_key`: (`vendor_id`, `slug`)

**Foreign keys:**

- `vendor_id` → `public.vendors`(`id`) — ON DELETE CASCADE

**Indexes:**

- `idx_vendor_products_active` — `CREATE INDEX idx_vendor_products_active ON public.vendor_products USING btree (id) WHERE (deleted_at IS NULL)`
- `idx_vendor_products_category` — `CREATE INDEX idx_vendor_products_category ON public.vendor_products USING btree (category)`
- `idx_vendor_products_name_trgm` — `CREATE INDEX idx_vendor_products_name_trgm ON public.vendor_products USING gin (lower(name) gin_trgm_ops)`
- `idx_vendor_products_needs_review` — `CREATE INDEX idx_vendor_products_needs_review ON public.vendor_products USING btree (needs_review) WHERE (needs_review = true)`
- `idx_vendor_products_vendor` — `CREATE INDEX idx_vendor_products_vendor ON public.vendor_products USING btree (vendor_id)`
- `vendor_products_vendor_id_slug_key` *(unique)* — `CREATE UNIQUE INDEX vendor_products_vendor_id_slug_key ON public.vendor_products USING btree (vendor_id, slug)`

---

### `vendors`

| Column | Type | Nullable | Default | Notes |
|---|---|---|---|---|
| `id` | `bigint` | NO | `nextval('vendors_id_seq'::regclass)` | **PK** |
| `name` | `text` | NO | — |  |
| `slug` | `text` | NO | — |  |
| `website` | `text` | YES | — |  |
| `notes` | `text` | YES | — |  |
| `needs_review` | `boolean` | NO | `false` |  |
| `deleted_at` | `timestamp with time zone` | YES | — |  |
| `created_at` | `timestamp with time zone` | NO | `now()` |  |
| `updated_at` | `timestamp with time zone` | NO | `now()` |  |

**Primary key:** `id`

**Unique constraints:**

- `vendors_slug_key`: (`slug`)

**Indexes:**

- `idx_vendors_active` — `CREATE INDEX idx_vendors_active ON public.vendors USING btree (id) WHERE (deleted_at IS NULL)`
- `idx_vendors_name_trgm` — `CREATE INDEX idx_vendors_name_trgm ON public.vendors USING gin (lower(name) gin_trgm_ops)`
- `idx_vendors_needs_review` — `CREATE INDEX idx_vendors_needs_review ON public.vendors USING btree (needs_review) WHERE (needs_review = true)`
- `idx_vendors_slug` — `CREATE INDEX idx_vendors_slug ON public.vendors USING btree (slug)`
- `vendors_slug_key` *(unique)* — `CREATE UNIQUE INDEX vendors_slug_key ON public.vendors USING btree (slug)`

---

